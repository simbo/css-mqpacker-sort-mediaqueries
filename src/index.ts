import * as SortMediaqueries from './sort-mediaqueries';

export const sortMediaqueries = SortMediaqueries.sortMediaqueries;
export default sortMediaqueries;
module.exports = SortMediaqueries.sortMediaqueries;
