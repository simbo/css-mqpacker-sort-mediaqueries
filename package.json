{
  "name": "css-mqpacker-sort-mediaqueries",
  "description": "Sorts mediaqueries by min-width and/or max-width, ordered ascending (to be used as sort option value for postcss plugin css-mqpacker)",
  "author": "Simon Lepel <simbo@simbo.de>",
  "license": "MIT",
  "keywords": [
    "css-mqpacker",
    "postcss"
  ],
  "version": "0.2.0",
  "repository": "https://github.com/simbo/css-mqpacker-sort-mediaqueries",
  "engines": {
    "node": "8.9.x"
  },
  "main": "dist/index.js",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "coverage": "istanbul cover ./node_modules/.bin/_mocha -- --reporter spec",
    "test": "mocha --reporter spec ./test/index.js"
  },
  "devDependencies": {
    "@types/node": "8.9.4",
    "array-shuffle": "^1.0.1",
    "istanbul": "^0.4.5",
    "mocha": "^5.0.1",
    "rimraf": "^2.6.2",
    "typescript": "^2.7.2"
  }
}
